# 📍 أين نحن — لوحة التتبّع

> افتح هذا الملف أي وقت تشعر فيه بالضياع. كل شيء محفوظ في `docs/` وعلى GitHub — لا تحتاج تتذكّر شيئاً.

**آخر تحديث:** ✅ **نواة US1 + التصحيح العميق مُثبَتة حيّاً** (gpt-5 حقيقي: تصحيح + شرح عربي + لا إفراط + رفض العبث).
**أنت الآن:** 🔐 **دوّر المفاتيح المسرَّبة أولاً**، ثم أغلِق فجوتَي US1 (نمط الاختبار + خط أساس eval) بلا أسرار جديدة.

> 🔐 تنبيه: مفاتيح OpenRouter و Supabase `service_role` ظهرت في المحادثة — **يجب تدويرها**.

---

## الرحلة كاملة (3 مراحل)

### المرحلة أ — التخطيط ✅ اكتملت
- [x] الرؤية والقرارات الكبرى → `docs/00`–`docs/10`
- [x] 14 سؤالاً مفتوحاً كلها محسومة → `docs/09`
- [x] الدستور (7 مبادئ) → `.specify/memory/constitution.md`
- [x] كل شيء مدفوع على GitHub

### المرحلة ب — Spec Kit لـ v1 (جارية الآن)
- [x] `specify` → `specs/001-v1-adaptive-tutor/spec.md`
- [x] `clarify` → 4 أسئلة مُجابة (المحتوى ضمن v1 · جلسة بمهلة خمول · إعدادات بذرة · أهداف بلغة طبيعية)
- [x] `plan` → plan.md + research.md + data-model.md + contracts/ + quickstart.md
- [x] `tasks` → tasks.md (56 مهمة، T001–T056، بعد إصلاح I1+U1)
- [x] `analyze` → نظيف: 0 حرج · 0 متوسّط متبقٍّ · 0 مخالفة دستور · تغطية 100%
- [x] اعتماد المهام
- [x] `implement` US1 (MVP) — نواة + تصحيح + تيليجرام، مبني ومدفوع
- [x] ✅ **تشغيل حيّ حقيقي نجح** — التصحيح العميق مُثبَت بـ gpt-5 (تصحيح+شرح، لا إفراط، رفض العبث)
- [ ] 🔐 **تدوير المفاتيح المسرَّبة ← الآن**
- [ ] إغلاق فجوتَي US1: نمط الاختبار (quiz/FR-002) + خط أساس eval (~12 حالة F0.5) — بلا أسرار جديدة
- [ ] نشر مسار DB+تيليجرام الكامل (يحتاج DATABASE_URL + توكن البوت + رابط HTTPS)
- [ ] ثم فقط: US2 (العمود المتكيّف)

### المرحلة ج — بناء v1 (بعد اعتماد tasks)
- [ ] نواة المحرّك (دوال نقية + اختبارات Vitest)
- [ ] طبقات AI (خلف Zod)
- [ ] عميل التيليجرام
- [ ] تأليف المنهج A1→A2 + المجموعة الذهبية (150+ حالة)
- [ ] بوابة التقييم (F0.5) في CI

---

## ✅ القاعدة التي تحميك
1. **لا كود قبل أن تعتمد أنت الـ `tasks`.**
2. كل مخرَج Spec Kit يُدفع على GitHub فور إنتاجه — مرجوع إليه دائماً.
3. أي قرار منسيّ؟ موجود في `docs/` — لا تحفظه، ارجع له.

---

## 👉 خطوتك الوحيدة التالية
في جلسة التنفيذ (الأخرى)، اكتب:

> أعتمد المهام (T001–T056). ابدأ التنفيذ بمسار **US1 (MVP)** فقط: النواة النقية + اختباراتها أولاً، ثم التصحيح العميق عبر التيليجرام. **توقّف عند اكتمال US1 واعرض شيئاً يعمل للتحقّق قبل التوسّع.** اختبر بالكود قبل الادّعاء، وادفع لـ GitHub بعد كل جزء.

نقطة التحقّق المطلوبة بعد US1: جرّب جُملاً تركية حقيقية — هل التصحيح يشرح السبب؟ هل المحلّل الصرفي يرفض العبث؟ هل لا يُفرط في التصحيح؟ ثم تعال هنا.
