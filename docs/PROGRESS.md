# 📍 أين نحن — لوحة التتبّع (دليل الرجوع)

> ⏸️ **المشروع موقوف مؤقّتاً** (المستخدم يركّز على مشاريع ثانية).
> **البوت حيّ وشغّال** — تقدر تستخدمه أي وقت. لمّا ترجع للتطوير، اقرأ هذا الملف وبتعرف كل شي.

---

## 🎉 وش تحقّق (البوت يشتغل فعلاً)
معلّم تركي بالـ AI، **حيّ على التيليجرام**، يصحّح بعمق ويشرح بالعربي:
- ✅ يصحّح أخطاءك التركية **ويشرح القاعدة بالعربي** (مثال: `okul`→`okula` + شرح حالة الاتجاه).
- ✅ **بوابة صرفية حتمية** ترفض الكلام التركي الخاطئ.
- ✅ بطاقات اختبار، حُكم نمط ذكي، SRS للمفردات.
- ✅ جودة التصحيح مقيسة: **F0.5 = 0.972، إفراط 0%**.
- ✅ يشرح بالعربي ويستخدم التركي فقط للكلمات المستهدفة (مناسب للمبتدئ A1).

## 🔧 أين يعمل (معلومات التشغيل)
- **الريبو:** https://github.com/junkpro518/turkce — كل شي مدفوع.
- **الرابط الحيّ:** https://turkce.mohammed518.com (webhook التيليجرام).
- **السيرفر:** `server-1` · المسار `/opt/projects/turkce` · Docker + Traefik (شبكة `proxy`, certresolver `myresolver`).
- **قاعدة البيانات:** Supabase (مشروع `mfunboybciyyilsupkdz`) عبر **Session pooler** (`aws-1-ap-northeast-1.pooler.supabase.com:5432`).
- **الأسرار:** في `/opt/projects/turkce/.env` على السيرفر (مُستبعَدة من git).
- **لإعادة النشر بعد أي تعديل:** `cd /opt/projects/turkce && git pull && docker compose up -d --build`.

---

## ✅ المنجَز
- **التخطيط كامل:** `docs/00`–`docs/10` · 14 سؤالاً محسوماً (`docs/09`) · الدستور (`.specify/memory/constitution.md`).
- **الميزة 001 (المعلّم الأساسي):** Spec Kit كامل → مبنية ومنشورة وحيّة. (`specs/001-v1-adaptive-tutor/`)
- **الميزة 002 (المعلّم المبادر):** Spec Kit كامل (specify/clarify/plan/tasks/analyze، 24 مهمة). (`specs/002-proactive-teacher/`)
  - **آخر شي قبل الإيقاف:** بناء **US1 منها = إصلاح "الصمت بعد الاختبار"** (يكمل الدرس بدل ما يسكت).

## ⏭️ لمّا ترجع — ابدأ من هنا (بالترتيب)
1. **أكمل الميزة 002 US2:** المبادرة الحقيقية (تذكير يومي + تنبيه لو غبت) — مجدول داخلي. (المهام جاهزة في `specs/002`.)
2. **الميزة 001 US2 (العمود المتكيّف):** يتذكّرك ويتكيّف — ربط الإتقان + السياق المتدرّج + ملخّص الجلسة + تأليف منهج A1→A2 الكامل + بوابة التقييم في CI.
3. **مراحل لاحقة:** ويب خفيف للوحات (1.5) · الصوت (2) · الانغماس بمحتوى تركي حقيقي (3).

## 🐞 مشاكل معروفة (رُصدت بالاستخدام الحيّ)
البوت شغّال لكن فيه 4 مشاكل مسجّلة بأسبابها المرجّحة في **[KNOWN_ISSUES.md](KNOWN_ISSUES.md)**: بطيء · يكرّر الأسئلة (غالباً بسبب البطء→إعادة محاولة webhook) · بعض الاختبارات تكشف إجابتها · يُنهي المحادثة عند الخطأ. تُحلّ عند الرجوع عبر Spec Kit.

## 🧹 متعلّقات صغيرة (لا تنساها)
- 🔐 **تدوير المفاتيح المسرَّبة:** مفتاح OpenRouter ومفتاح Supabase `service_role` ظهرا في المحادثة. **دوّرهما** (خصوصاً والبوت بيشتغل وأنت غايب). كلمة مرور القاعدة بدّلتها سابقاً ✅.
- إصلاح صغير: التطبيق يطبع توكن البوت في الـlogs عند الخطأ — يُحجَب لاحقاً.

---

## 🔑 طريقة العمل (كانت ناجحة — أكملها لمّا ترجع)
- **Spec Kit إلزامي لكل ميزة:** `specify → clarify → plan → tasks → analyze → implement`. لا كود خارج المسار.
- **لا كود قبل ما تعتمد أنت الـtasks.**
- **التنفيذ في جلسة منفصلة؛ هذه الجلسة للتخطيط والإرشاد.**
- كل شي مدفوع على GitHub — لا تحفظ شي براسك، ارجع للوثائق.

> دليل المشروع الكامل: [docs/README.md](README.md).
